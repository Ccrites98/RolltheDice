package com.pluralsight;
import java.util.*;
//Roll the dice Jamie
public class Dice {

        //Jamie, set those variables up dude

    public int roll() {
        int random = 1 + (int) (Math.random() * 6);
   return random; }
}
